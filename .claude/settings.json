{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | { read -r cmd; if echo \"$cmd\" | grep -qE '^npm\\s+install'; then export npm_config_user_agent=pnpm; fi; }",
            "timeout": 5
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": ["Bash(pnpm *)", "Bash(node *)", "Bash(git *)", "Read", "Edit", "Write", "Glob", "Grep"]
  }
}